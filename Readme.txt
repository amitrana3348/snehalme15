The final code is hello-template.py

the html file is stored in templates folder, called html2

all the lirbaries are installed and the code is working perfect, if there are any problems, then respective hardware connection is needed to be checked

code compleded on 4th June 2015

The dependancies that are installed are

apt-get update, upgrade

rpi-gpio library

adafruit library

adafruit BMP library

flask library module

